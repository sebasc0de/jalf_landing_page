export interface RRSS {
  id: number;
  url: string;
  icon: string;
}

export interface Step {
  id: number;
  img: string;
  title: string;
  desc: string;
  short_desc: string;
  videoUrl: string;
}
